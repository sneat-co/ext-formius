# Formius extension contract

Public TypeScript and Angular contract for the Sneat Formius extension.

It publishes `@sneat/extension-formius-contract`, including form/list DTOs,
contexts, service interfaces, and injection tokens. Private Formius runtime and
UI implementation stays in the `formius` repository.
